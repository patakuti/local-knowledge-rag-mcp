# Environment Variables Configuration
#
# To use this file:
# 1. Copy this file to .env in the project root:
#    cp .env.example .env
# 2. Edit .env and set your actual values
# 3. The .env file is automatically loaded when the MCP server starts
#
# Note: Environment variables can also be set in your Claude Desktop/Cline configuration,
# but using .env file is more convenient for local development.

# PostgreSQL Database Connection
# Required for storing vector embeddings
DATABASE_URL=postgresql://user:password@localhost:5432/local_knowledge_rag

# Embedding Provider Configuration
# Choose one of the following providers:

# Option 1: OpenAI
OPENAI_API_KEY=sk-your-openai-api-key
# Optional: Specify a different model (default: text-embedding-3-small)
# EMBEDDING_MODEL=text-embedding-3-large

# Option 2: Ollama (local)
# OLLAMA_BASE_URL=http://localhost:11434/v1
# EMBEDDING_MODEL=nomic-embed-text

# Option 3: OpenAI-compatible APIs (Azure OpenAI, local LLMs, etc.)
# OPENAI_COMPATIBLE_API_KEY=your-api-key
# OPENAI_COMPATIBLE_BASE_URL=https://your-endpoint.com/v1
# EMBEDDING_MODEL=your-embedding-model

# RAG Configuration (Optional - can also be configured per workspace in Console UI)
# All settings have sensible defaults and can be overridden per workspace

# Chunking settings
# RAG_CHUNK_SIZE=1000                    # Chunk size in characters
# RAG_CHUNK_OVERLAP=200                  # Chunk overlap in characters
# RAG_CHUNK_LANGUAGE=markdown            # Chunking language
# RAG_EXCLUDE_CODE_LANGUAGES=javascript,js,jsx,typescript,ts,tsx,python,py,java,kotlin,scala,c,cpp,c++,cxx,cc,h,hpp,csharp,cs,ruby,rb,php,go,golang,rust,rs,swift,perl,lua,r,matlab,dot,graphviz
#   # Code block languages to exclude from Markdown indexing (comma-separated, case-insensitive)
#   # Default: Common programming languages (JS, TS, Python, Java, C/C++, etc.)
#   # Included: PlantUML, Mermaid, YAML, JSON, Shell, SQL, etc.
#   # Set to empty string to index all code blocks: RAG_EXCLUDE_CODE_LANGUAGES=

# Search settings
# RAG_MIN_SIMILARITY=0.7                 # Minimum similarity threshold (0.0-1.0)
# RAG_MAX_RESULTS=10                     # Maximum number of search results
# RAG_MAX_CHUNKS_PER_QUERY=5             # Maximum chunks per query

# Indexing settings (comma-separated patterns)
# RAG_INCLUDE_PATTERNS="**/*.md,**/*.txt"
# RAG_EXCLUDE_PATTERNS="**/node_modules/**,**/.git/**"

# Report settings
# RAG_REPORT_OUTPUT_DIR=rag-reports      # Report output directory (default: rag-reports)
# RAG_MAX_QUOTE_LINES=5                  # Maximum lines in quotes
# RAG_REMOVE_BLANK_LINES=true            # Remove blank lines in reports
# RAG_DEFAULT_TEMPLATE=basic             # Default template (basic/paper/bullet_points)

# Session settings
# RAG_MAX_SESSION_RESULTS=10             # Maximum number of cached sessions

# Debug settings
# RAG_DEBUG_LOG_ENABLED=false            # Enable debug logging
# RAG_DEBUG_LOG_PATH=/tmp/local-knowledge-rag-mcp/debug.log  # Debug log file path
